# render-workflows-harness-e2e

E2E test app for the pydantic-ai-harness RenderWorkflows integration.
Deployed as a Render Workflow service (Python runtime).

- Build: `pip install -r requirements.txt`
- Run: `python hosted_app.py`
